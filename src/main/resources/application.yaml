spring:
  application:
    name: accounts

  datasource:
    url: jdbc:mysql://localhost:3306/option1
    username: root
    password: Ksiddu0575@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always